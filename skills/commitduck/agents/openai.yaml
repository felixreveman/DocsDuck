interface:
  display_name: "CommitDuck"
  short_description: "Sync documentation before Git commits"
  default_prompt: "Use $commitduck to review the current changes, update affected documentation, and prepare a safe local commit."
